import { OpenerSetting } from "./types";

export const DEFAULT_SETTINGS: OpenerSetting = {
	newTab: true,
	PDFApp: true,
	extOnlyWhenMetaKey: true,
	allExt: false,
	custExt: false,
	custExtList: [],
};
